// Re-exported from the shared design system (@mela/ui-web). Kept here so existing
// `@/components/ui/Button` imports keep working while the implementation is shared.
export { Button, type ButtonProps } from '@mela/ui-web'
