# Hoist nothing by default for stricter dependency hygiene, but allow common
# build/runtime peers to be hoisted so tooling (prisma, vite, tsx) resolves cleanly.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
